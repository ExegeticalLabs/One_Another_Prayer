# Pilot Operations Plan v1

## Warning: Not Production-Ready

This application is moving into **Controlled MVP Testing** locally and manually. It is explicitly NOT production-ready for real congregations yet due to AI key client exposures natively mapped over frontend components and client-directed DB increments natively opening abuse risks. 

## Testing Stages

### 1. Fake-Data Internal Test (Current Phase)
- **Goal:** Execute `ACCEPTANCE_MATRIX.md` natively in isolation validating roles using Sandbox configurations.
- **Participants:** Dedicated developers or single product owners.
- **Environment:** Localhost utilizing `seed.js`/`cleanup.js` test injections running cleanly inside `church_grace_test` testing. 

### 2. Trusted Small-Group Test
- **Prerequisites:** 
  1. Relocating `@google/genai` payload calls securely off Native Client components explicitly into Cloud Functions or declaring formally documented Accepted Temporary Risk tolerances if bounding tightly physically in closed physical scopes.
  2. Adding explicit `localStorage.clear()` boundaries natively capturing `Sign-Out` hooks preventing shared-device journaling leaks implicitly.
- **Goal:** Real devices fetching real endpoints simultaneously verifying multi-account sync behaviors correctly handling realtime feed sorting arrays resolving locally physically. 
- **Participants:** 3-5 users holding explicit specific instruction documentation. 

### 3. Real Church Pilot
- **Prerequisites:**
  1. Backend transaction routines converting `increment` scripts. 
  2. Full security rule audits verifying roster protections against general users globally scanning.
- **Duration:** 2-4 Weeks.
- **Action:** Pilot church uses internally without publishing general community invites actively yet.

## Operational Procedures 

### Bug Reporting & Issues
All errors are collected physically utilizing the `BUG_REPORT_TEMPLATE.md` constraints analyzing the exact physical role scopes failing dynamically ensuring specific rule violations are caught securely protecting data. 

### Pastoral Escalation Handlers 
If Gemini API flags an explicit natively recorded `URGENT` notification, the application purely visualizes the flag within the `<AdminPanel>`. The app **DOES NOT** physically emit native SMS/Email hooks automatically alerting sleeping pastors. Elders must physically log in validating datasets.

### Go/No-Go Launch Checks
Before launching standard groups natively:
* Does the app feel distinctly unique representing sacred space, or does the chronological feed behavior revert implicitly tracking similar dopamine loop architectures represented in standard social apps? 

*If it feels like social media natively, it breaches Foundation v5.*
